<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Files</title>
</head>
<body>
    <h1>Upload Logo, Favicon, and Research Image</h1>

    <!-- Form for uploading multiple files -->
    <form action="/upload-images" method="POST" enctype="multipart/form-data">
        <!-- Input for Logo -->
        <label for="logo">Upload Logo:</label>
        <input type="file" name="logo" accept="image/*"><br><br>

        <!-- Input for Favicon -->
        <label for="favicon">Upload Favicon:</label>
        <input type="file" name="favicon" accept="image/*"><br><br>

        <!-- Input for Research Image -->
        <label for="researchImage">Upload Research Image:</label>
        <input type="file" name="researchImage" accept="image/*"><br><br>

        <!-- Submit button -->
        <button type="submit">Upload Images</button>
    </form>

    <!-- Display uploaded images if they exist -->
    <% if (images) { %>
        <h2>Uploaded Images:</h2>
        <div>
            <% if (images.logoUrl) { %>
                <p><strong>Logo:</strong></p>
                <img src="<%= images.logoUrl %>" alt="Logo" style="max-width: 150px;">
            <% } %>
            <% if (images.faviconUrl) { %>
                <p><strong>Favicon:</strong></p>
                <img src="<%= images.faviconUrl %>" alt="Favicon" style="max-width: 32px;">
            <% } %>
            <% if (images.researchImageUrl) { %>
                <p><strong>Research Image:</strong></p>
                <img src="<%= images.researchImageUrl %>" alt="Research Image" style="max-width: 300px;">
            <% } %>
        </div>
    <% } %>

</body>
</html>
