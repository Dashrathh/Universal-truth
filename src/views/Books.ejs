<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Details</title>
    <link rel="icon" type="image/png" sizes="50x50" href="https://i.imgur.com/PMDKyca.png">

    <style>
        /* General styles */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 80%;
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
            background: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        /* Title and year styles */
        .title {
            font-size: 2em;
            color: #2c3e50;
            margin-bottom: 10px;
            text-align: center;
        }

        .year {
            font-size: 1.2em;
            color: #7f8c8d;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Paragraph styles */
        .info, .invention {
            font-size: 1.1em;
            margin-bottom: 15px;
            text-align: justify;
        }

        /* Invention heading */
        .invention-heading {
            font-size: 1.4em;
            margin-top: 20px;
            margin-bottom: 15px;
            color: #34495e;
            text-align: center;
        }

        /* Image styles */
        .book-image {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            display: block;
            margin: 20px auto;
            transition: transform 0.3s ease-in-out;
        }

        .book-image:hover {
            transform: scale(1.05);
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .container {
                width: 90%;
                padding: 15px;
            }

            .title {
                font-size: 1.8em;
            }

            .year {
                font-size: 1em;
            }

            .info, .invention {
                font-size: 1em;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="title"><%= book.title %></h2>
        <h2 class="year"><%= book.written_year %></h2>
        <p class="info"><%= book.BookInfo %></p>
        <img class="book-image" src="<%= book.BookImage %>" alt="Cover Image of <%= book.title %>">
        <p class="invention-heading">Ancient Invention</p>
        <p class="invention"><%= book.Ancient_invention %></p>
        <img class="book-image" src="<%= book.Ancient_inventionImage %>" alt="Image of <%= book.Ancient_invention %>">
        <p class="info"><%= book.description %></p>
    </div>
</body>

</html>
